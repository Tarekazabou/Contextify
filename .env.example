# Contextify Configuration
# Copy this to .env and fill in your values

# Required: Get your API key from https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-api-key-here

# Optional: Maximum number of files to include (default: 30)
# CONTEXTIFY_MAX_FILES=30

# Optional: Default focus mode (frontend, backend, database, config, tests)
# CONTEXTIFY_DEFAULT_FOCUS=

# Optional: Always include changed files only
# CONTEXTIFY_CHANGED_ONLY=false
